package hasanalmunawrDev.jwt.controller;

import hasanalmunawrDev.jwt.service.LogoutHandlerService;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.ResponseEntity;
import org.springframework.security.core.Authentication;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
//@RequestMapping(path = "/api")
public class LogoutController {

//    @Autowired
//    private LogoutHandlerService logoutHandlerService;

//    @GetMapping("/logout")
//    public void (HttpServletRequest request, HttpServletResponse response, Authentication authentication) {
//        logoutHandlerService.logout(request, response, authentication);
    }
